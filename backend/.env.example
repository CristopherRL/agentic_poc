# OpenAI credentials
OPENAI_API_KEY=sk-your-openai-key

# Admin token for rate limit management endpoints
ADMIN_TOKEN=your-secret-admin-token-here

# CORS origins (comma-separated, for production)
# Example: CORS_ORIGINS=https://your-frontend.vercel.app,https://another-domain.com
CORS_ORIGINS=

# Rate limiting settings
ENABLE_RATE_LIMIT=true
DAILY_INTERACTION_LIMIT=20

# Optional: Override default paths (usually not needed)
# PROJECT_ROOT=.
# CSV_DIR=docs/public/data
# SQLITE_PATH=data/db/app.db
# DOCS_DIR=docs/public/docs
# VDB_DIR=data/vdb

# Optional: Override LLM models (usually not needed)
# ROUTER_LLM_MODEL=gpt-4o-mini
# SQL_LLM_MODEL=gpt-4o-mini
# SYNTHESIS_LLM_MODEL=gpt-4o
# SPLIT_LLM_MODEL=gpt-4o

# Optional: Required data tables for SQL queries (JSON array)

# REQUIRED_DATA_TABLES=["DIM_MODEL","DIM_COUNTRY","DIM_ORDERTYPE","FACT_SALES","FACT_SALES_ORDERTYPE"]
